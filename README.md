# credit-fraud-detection-submission
A complete fraud detection pipeline in Python, featuring data preprocessing, machine learning models (RandomForest, CatBoost), evaluation metrics, and output generation (plots, warehouse tables). Outputs align with the deployed backend for consistency. Built with scikit-learn, pandas, and matplotlib.
